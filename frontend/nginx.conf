server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Serve files if they exist, otherwise fall back to SPA entry
  location / {
    try_files $uri $uri/ /index.html;
  }
}
